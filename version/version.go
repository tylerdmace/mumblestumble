package version

var (
	// BuildVersion is supplied by the build script in the project root
	BuildVersion string
	// BuildTimestamp is supplied by the build script in the project root
	BuildTimestamp string
)
